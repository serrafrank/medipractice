management:
    endpoints:
        web:
            exposure:
                include: '*'
spring:
    application:
        name: ${project.name}
    cloud:
        config:
            uri: http://localhost:9101

logging:
    file: logs/@project.name@.log